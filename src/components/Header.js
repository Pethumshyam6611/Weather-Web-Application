import React from 'react';
import '../styles/Header.css';

const Header = () => {
  return (
    <header className="app-header">
      <h1>Futuristic Weather</h1>
      <div className="settings-icon">⚙️</div>
    </header>
  );
};

export default Header;
